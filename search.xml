<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>Hexo博客搭建</title>
      <link href="/2022/01/29/hexo-bo-ke-da-jian/"/>
      <url>/2022/01/29/hexo-bo-ke-da-jian/</url>
      
        <content type="html"><![CDATA[<h2 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h2><ul><li><p>本内容主要介绍我在搭建这个博客时的出现一系列的问题，当时也是费了半天功夫才搭建好，在这篇文章中会详细介绍整个流程，尽量避免入坑吧，但要是真是你自己给自己挖坑那就没办法了。</p></li><li><p>搭建的时候也是看着别人的教程一点点跟进，但是都有缺陷。<br>比如分支的创建，和私人域名的解析，有这方面的教程但是都不是太完善，有些只是教你搭建但是后期没有考虑换电脑，换环境的情况。包括后期维护等等。</p></li><li><p>优点</p><ul><li>不需要服务器，<code>github</code>免费提供的托管服务</li><li>不需要域名，<code>github</code>同样提供了一个.<code>io</code>的域名</li><li>不用自己写 <code>xml</code>、<code>hexo</code> 提供了很多开源的博客模板。</li><li>便于维护更新，换风格直接换模板就行。</li></ul></li></ul><p>##下面进入正题##</p><hr><h2 id="内容"><a href="#内容" class="headerlink" title="内容"></a>内容</h2><h3 id="安装相关工具"><a href="#安装相关工具" class="headerlink" title="安装相关工具"></a>安装相关工具</h3><h4 id="安装Node-js"><a href="#安装Node-js" class="headerlink" title="安装Node.js"></a>安装<code>Node.js</code></h4><p><a href="https://nodejs.org/dist/v4.2.3/node-v4.2.3-x86.msi">https://nodejs.org/dist/v4.2.3/node-v4.2.3-x86.msi</a></p><p><a href="https://nodejs.org/dist/v4.2.3/node-v4.2.3-x64.msi">https://nodejs.org/dist/v4.2.3/node-v4.2.3-x64.msi</a></p><p>选择需要的版本下载，分别是32位和64位。下载后安装一直默认下一步就行。</p><h4 id="安装Git"><a href="#安装Git" class="headerlink" title="安装Git"></a>安装Git</h4><p><code>Git-2.11.0.3-32-bit.exe Git-2.11.0.3-64-bit.exe</code><br>打开<code>cmd</code>命令行(<code>win+r</code> 输入<code>cmd</code>回车)分别执行</p><pre class="line-numbers language-none"><code class="language-none">node   -vnpm    -vgit    --version<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>如果都可以成功运行出现版本信息证明安装成功。<br>这里写过一篇介绍git的文章，如果安装遇到问题可以去查看：</p><p><a href="https://www.jianshu.com/p/555e7a188312">https://www.jianshu.com/p/555e7a188312</a></p><h4 id="安装Hexo-并初始化"><a href="#安装Hexo-并初始化" class="headerlink" title="安装Hexo,并初始化"></a>安装<code>Hexo</code>,并初始化</h4><p>必须按照步骤来，因为<code>hexo</code>需要使用<code>node.js</code>的<code>npm</code>命令。</p><p>打开<code>cmd</code>命令行(<code>win+r </code>输入<code>cmd</code>回车)分别执行</p><pre class="line-numbers language-none"><code class="language-none">npm install hexo-cli -g<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>等待自动安装完成后。输入</p><pre class="line-numbers language-none"><code class="language-none">hexo -v<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>检查是否安装成功</p><p>到这一步如果所有都安装成功，并测试无问题，那么本地配置已经搞定，下面开始在<code>github</code>上建项目了。</p><p>在<code>github</code>上创建并设置远程库</p><p>注册登录略过，不会的请自行百度。</p><p>登录后点击“+”号，选择New repository，下一步</p><p>在Repository name下面填写你要创建的地址，这个地址是就是你的域名，以<code>github.io</code>结尾。**例如填写<code>wapchief.github.io</code>以后就在地址栏直接输入</p><p><code>wapchief.github.io</code>就可以访问网站</p><p>按照提示一步步完成创建。</p><p>创建之后，点击setting设置远程库：</p><p>找到<code>GitHub Pages</code>点击<code>Automatic page generator</code></p><p>等待<code>github pages</code>创建完成，然后输入你创建的地址测试<code>github pager</code>是否创建成功。此时页面是空的没有任何东西。</p><p>下面要做的就是使用<code>hexo</code>模板将源码放到<code>github</code>。</p><p>#####初始化<code>Hexo</code></p><p>创建一个文件夹用于存放<code>hexo</code>。然后右键该文件夹使用</p><pre class="line-numbers language-none"><code class="language-none">git bash here<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>命令就可以直接定位到当前目录。</p><p>右键根文件夹，点击<code>git bash here</code></p><p>然后使用</p><pre class="line-numbers language-none"><code class="language-none">npm install hexo --save<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>初始化<code>hexo</code>基础配置文件。初始化时间比较长，不用急，等完成后输入</p><pre class="line-numbers language-none"><code class="language-none">hexo init<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>初始化<code>hexo</code>。这个命令和git 仓库命令相似。意思一样都是初始化。</p><p>接着输入</p><pre class="line-numbers language-none"><code class="language-none">npm install<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>配置<code>node</code>。然后输入</p><pre class="line-numbers language-none"><code class="language-none">hexo g<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p><code>加载hexo基础html、css、js等文件。</code></p><p>在这完成后等于已经在本地创建了一个网页，想查看的话，输入</p><pre class="line-numbers language-none"><code class="language-none">hexo s<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>然后相当于开启了一个本地的服务器，会提示你拷贝浏览器。</p><p>在浏览器输入 <a href="http://localhost:4000/%EF%BC%8C%E5%8F%AF%E4%BB%A5%E7%99%BB%E9%99%86%E4%B8%8A%E5%8E%BB%E8%AF%81%E6%98%8E%E6%9C%AC%E5%9C%B0%60hexo%60%E9%BB%98%E8%AE%A4%E6%A8%A1%E6%9D%BF%E9%85%8D%E7%BD%AE%E6%88%90%E5%8A%9F%EF%BC%8C%E5%88%B0%E8%BF%99%E4%B8%80%E6%AD%A5%E5%A6%82%E6%9E%9C%E5%B0%86%60hexo%60%E6%94%BE%E5%88%B0%60github">http://localhost:4000/，可以登陆上去证明本地`hexo`默认模板配置成功，到这一步如果将`hexo`放到`github</a> page<code>,那么使用</code>github.io`访问的</p><p>将是一个默认的页面。下面我们要把他<code>diy</code>成自己的博客，包括导入模板主题等。</p><h4 id="修改内容"><a href="#修改内容" class="headerlink" title="修改内容"></a>修改内容</h4><p>内容较多建议参考官方文档</p><p><a href="https://hexo.io/zh-cn/docs/configuration.html">https://hexo.io/zh-cn/docs/configuration.html</a></p><p>修改完之后，可以重新执行<code>hexo s</code>在浏览器查看效果。并确认无误，包括以后需要添加文章，或者更新主题等，都建议先在本地查看无误再远程部署。</p><p>根_<code>config.yml</code>文件中</p><p>这里有几个坑需要注意一下：</p><ul><li><p>所有的配置“:”符号后面都要带空格，否则执行本地测试直接失败。</p></li><li><p>language是设置语言。<code>zh-Hans</code>是中文。</p></li><li><p>如果设置<code>zh-Hans</code>后仍出现乱码问题。需要更改文件的字符编码集为<code>UTF-8</code>,方法很多具体，就不详细介绍了。</p></li></ul><p>注意同时要在，根_config.yml中配置自己的远程仓库地址</p><pre class="line-numbers language-none"><code class="language-none">#Deployment##Docs: https://hexo.io/docs/deployment.htmldeploy:type: git  repo: https://github.com/wapchief/wapchief.github.io.git  branch: main<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>这一步很非常关键，不要漏了。(上传本地文件)</p><hr><h3 id="提交项目，部署"><a href="#提交项目，部署" class="headerlink" title="提交项目，部署"></a>提交项目，部署</h3><p>继续在根目录命令行安装部署工具（方便以后更新）</p><pre class="line-numbers language-none"><code class="language-none">npm install hexo-deployer-git -save<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li>初始化本地仓库：</li></ul><pre class="line-numbers language-none"><code class="language-none">git init<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li>连接远程仓库：<br>如果是第一次使用git，在使用git的时候会提示输入用户名和密码，用户名是自己的注册邮箱。</li></ul><pre class="line-numbers language-none"><code class="language-none">git remote add origin https://github.com/wapchief/wapchief.github.io.git<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li>发布<code>hexo</code>到<code>github page</code></li></ul><pre class="line-numbers language-none"><code class="language-none">hexo clean &amp;&amp; hexo g &amp;&amp; hexo d<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>等价于</p><pre class="line-numbers language-none"><code class="language-none">hexo  cleanhexo  ghexo  d<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><ul><li>添加文件到本地仓库</li></ul><pre class="line-numbers language-none"><code class="language-none">git add<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li>提交声明</li></ul><pre class="line-numbers language-none"><code class="language-none">git commit -m '内容'<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li>推送到远程仓库（<code>github</code>）</li></ul><p><code>这里建议创建一个新的分支hexo，用于管理hexo文件。提交的的时候只提交hexo网站html、css、等源文件。而默认的master用来部署更新项目,具体可以</code></p><p><code>看我的github地址分支情况https://github.com/wapchief/wapchief.github.io</code></p><ul><li>创建并切换到新建分支：</li></ul><pre class="line-numbers language-none"><code class="language-none">git checkout -b hexo<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li>将分支推送到远程仓库：</li></ul><pre class="line-numbers language-none"><code class="language-none">git push origin hexo<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><ul><li><p>这时打开网站就能看到效果了。</p></li><li><p>记得提交以后去<code>github</code>上把<code>hexo</code>分支设置默认，以后写文章等都要部署。</p></li><li><p>文章在<code>hexo</code>目录下的\source_posts文件夹中，是<code>md</code>格式，也就是Markdown格式。</p></li><li><p>以后可以用以下命令部署。也就是第三步</p></li></ul><p>//等于一次性执行了，清空、刷新、部署三个命令</p><pre class="line-numbers language-none"><code class="language-none">hexo clean &amp;&amp; hexo g &amp;&amp; hexo d<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h3 id="注意"><a href="#注意" class="headerlink" title="注意"></a>注意</h3><p>如果部署时不clean，可能之前修改的文章还存在。包括一些配置等，有时候部署完成后并没有改变，其实是缓存未清除。</p><hr><h3 id="进阶-绑定域名"><a href="#进阶-绑定域名" class="headerlink" title="进阶-绑定域名"></a>进阶-绑定域名</h3><p>域名可以自行去阿里云，景安等网站购买。一般都有系统提示步骤。</p><p>拿到域名之后，进行解析，推荐使用<code>dnspod</code>免费解析。</p><p>具体使用方法：</p><ul><li><p>注册登录<code>dnspod</code>之后，添加域名，然后解析，添加两条记录</p></li><li><p>登录你注册域名网站的管理后台，修改<code>DNS</code>解析地址为</p></li></ul><pre class="line-numbers language-none"><code class="language-none">f1g1ns1.dnspod.netf1g1ns2.dnspod.net<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><ul><li><p>登录<code>github</code>，找到项目，设置<code>setting</code>，找到<code>GitHub Pages</code></p></li><li><p>在master分支下的<code>CNAME.MD</code>文件写入注册的域名</p><p>再次访问差不多就好了，如果延迟的话，可能解析立马不会生效。需要等会儿才能看到。</p></li></ul><hr><h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ul><li>制作不易</li></ul><p>自己创建博客已经有些日子了，现在朋友想搭建博客，正好自己接触过，也整理一下，以后方便查看。所有的流程都在这里。</p><p>对于有<code>git、nodeJs</code>基础的人来说搭建起来可能也就是十几分钟的事，但是对于没接触过的就要费很大工夫。如果遇到问题欢迎加我的联系方式反馈。</p>]]></content>
      
      
      
        <tags>
            
            <tag> thchnology </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Hello World</title>
      <link href="/2022/01/28/hello-world/"/>
      <url>/2022/01/28/hello-world/</url>
      
        <content type="html"><![CDATA[<h1 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h1><ul><li>这是我第一篇<code>Hexo</code>博客文章</li></ul><h2 id="内容"><a href="#内容" class="headerlink" title="内容"></a>内容</h2><ul><li>这个网站的第一篇博客<code>Hexo</code>我想了很久，比如说内容写什么，之类的一系列问题，而最大的问题就是我不知道第一篇博客应该写什么名字，比如标题应该是命为<code>我的第一篇博客</code>呢，还是应该叫<code>Hello World</code>呢，这个问题让我想了三四天，最后还是将这篇文章命名为<code>Hello World</code>，因为自古以来程序员第一个内容就应该是<code>Hello World</code>所以最后还是将标题设置为了<code>Hello World</code></li><li>虽然我不是很强的技术大牛，但是我也渡过了非常艰难的小白空档期，这一步步的无疑是我一直坚持下去的成果，有最新的技术也会在博客上面投稿</li></ul>]]></content>
      
      
      
        <tags>
            
            <tag> Hello World </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
